o but then how/where to check out ptags? as a plugin for nvim?
    - and then things have to be in ./lua, not nvim/lua ... doesnt quite fit
    o ok pragmatic, make it an nvim plugin, main use-case anyway
